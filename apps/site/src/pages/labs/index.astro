---
import SiteLayout from '../../layouts/SiteLayout.astro';
import { isPreview } from '../../lib/deployment';
const tools = [
  {
    name: 'Interference field',
    href: '/labs/interference/',
    kind: 'Shader',
    description: 'Adjust two wave sources and inspect the resulting field.',
  },
  {
    name: 'Theme studio',
    href: '/labs/design-studio/',
    kind: 'Design tool',
    description:
      'Try fonts, inks, and board details. Copy or download your choices.',
  },
];
const studies = [
  {
    name: 'Field studies',
    href: '/labs/field-studies/',
    description: 'Flux core and three other layered sculptures.',
  },
  {
    name: 'Mathematical sculptures',
    href: '/labs/sculpture-studies/',
    description: 'Enneper, Gyroid, Dipole, and a planetary gear stage.',
  },
  {
    name: 'Hero studies',
    href: '/labs/hero-studies/',
    description: 'Sculpture, shader, and illustration compositions.',
  },
  {
    name: 'Homepage studies',
    href: '/labs/homepage-studies/',
    description: 'Three earlier approaches to the homepage.',
  },
  {
    name: 'Board studies',
    href: '/labs/board-studies/',
    description: 'Whiteboard and blackboard treatments.',
  },
];
---

<SiteLayout
  title="Labs"
  description="Small tools and interactive experiments built with Alkemist."
  preview={isPreview}
>
  <header class="labs-heading">
    <h1>Labs</h1><p class="alk-lead">
      Small tools and interactive experiments.
    </p>
  </header>
  <div class="labs-tools">
    {
      tools.map((tool) => (
        <a class="labs-tool" href={tool.href}>
          <>
            <span class="labs-kind">{tool.kind}</span>
            <h2>
              {tool.name} <span aria-hidden="true">↗</span>
            </h2>
            <p>{tool.description}</p>
          </>
        </a>
      ))
    }
  </div>
  <p class="labs-components">
    Looking for charts, models, or code examples? <a href="/test/"
      >Open the test page →</a
    >
  </p>
  <section class="labs-history" aria-labelledby="history-title">
    <div>
      <h2 id="history-title">Design notebook</h2><p>
        The studies behind this site.
      </p>
    </div>
    <details>
      <summary
        >Browse the design studies <span>{studies.length} studies</span
        ></summary
      ><ul>
        {
          studies.map((study) => (
            <li>
              <>
                <a href={study.href}>
                  {study.name} <span aria-hidden="true">↗</span>
                </a>
                <p>{study.description}</p>
              </>
            </li>
          ))
        }
      </ul>
    </details>
  </section>
</SiteLayout>

<style>
  .labs-heading {
    margin-bottom: 2rem;
  }
  .labs-heading h1 {
    margin-bottom: 0.65rem;
  }
  .labs-heading p {
    margin: 0;
  }
  .labs-tools {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }
  .labs-tool {
    display: block;
    padding: 1.4rem;
    border: 1px solid var(--alk-rule);
    border-radius: 4px;
    text-decoration: none;
  }
  .labs-tool:hover {
    border-color: var(--alk-muted);
  }
  .labs-kind {
    color: var(--alk-ink-cobalt);
    font: 0.72rem var(--alk-mono);
  }
  .labs-tool:nth-child(2) .labs-kind {
    color: var(--alk-ink-teal);
  }
  .labs-tool h2 {
    display: flex;
    justify-content: space-between;
    gap: 1rem;
    font-size: 1.3rem;
    margin: 0.9rem 0 0.7rem;
  }
  .labs-tool h2 span {
    font: 1rem var(--alk-mono);
  }
  .labs-tool p {
    color: var(--alk-muted);
    font-size: 0.95rem;
    margin: 0;
    max-width: 44ch;
  }
  .labs-components {
    color: var(--alk-muted);
    font-size: 0.85rem;
    margin: 1.3rem 0 0;
  }
  .labs-history {
    border-top: 1px solid var(--alk-rule);
    margin-top: 3rem;
    padding-top: 1.7rem;
    display: grid;
    grid-template-columns: 0.65fr 1.35fr;
    gap: 2rem;
  }
  .labs-history h2 {
    font-size: 1.3rem;
    margin: 0 0 0.4rem;
  }
  .labs-history p {
    color: var(--alk-muted);
    font-size: 0.9rem;
    margin: 0;
  }
  details {
    padding: 0;
    border: 0;
  }
  summary {
    cursor: pointer;
    padding: 0.2rem 0;
    font-size: 0.95rem;
  }
  summary span {
    float: right;
    color: var(--alk-muted);
    font: 0.75rem/1.7 var(--alk-mono);
  }
  ul {
    list-style: none;
    padding: 0;
    margin: 1.3rem 0 0;
  }
  li {
    border-top: 1px solid var(--alk-rule);
    padding: 0.8rem 0;
    margin: 0;
  }
  li a {
    font-weight: 500;
    font-size: 0.95rem;
    text-decoration: none;
  }
  li a:hover {
    text-decoration: underline;
  }
  li p {
    margin-top: 0.3rem;
  }
  @media (max-width: 650px) {
    .labs-tools,
    .labs-history {
      grid-template-columns: 1fr;
    }
    .labs-history {
      gap: 1rem;
      margin-top: 2rem;
    }
    .labs-tool {
      padding: 1.15rem;
    }
  }
</style>
