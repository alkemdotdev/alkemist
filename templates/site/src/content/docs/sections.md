---
title: Sections
description: How the site-local section configuration shapes the public site.
order: 2
---

Every configured section has an `enabled` flag and a label. Disable a section
to remove its navigation item and generated routes; rename its label without
changing its stable URL.
