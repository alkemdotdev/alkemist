---
import './shader.css';
import './parameters.css';
import './figure.css';
import type { ParametersProp } from './parameters';
export interface ShaderProps {
  id?: string;
  title?: string;
  /** Spatial frequency, clamped to the visible control's range of 3–18. */
  frequency?: number;
  /** Rotation of the two sources in degrees, clamped to 0–180. */
  angle?: number;
  /** Controls to expose locally; defaults to frequency and source angle. */
  parameters?: ParametersProp<'frequency' | 'angle'>;
  class?: string;
}
type Props = ShaderProps;
import { renderShader } from './visualization-renderers';
---

<Fragment set:html={renderShader(Astro.props)} />

<script>
  import './shader-client';
</script>
