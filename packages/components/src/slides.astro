---
import Deck from './deck.astro';
import type { DeckProps } from './deck.astro';
/** Compatibility entry for authored decks. Prefer Deck for new code. */
export type SlidesProps = DeckProps;
type Props = SlidesProps;
---

<Deck {...Astro.props}><slot /></Deck>
